/*
using System;

class SeriesDemo
{
    static void Main()
    {
        ByTwos ob = new ByTwos();

        for (int i = 0; i < 5; i++)
            Console.WriteLine("Next Value is " + ob.GetNext());

        Console.WriteLine("\nResetting");
        ob.Reset();
        for (int i = 0; i < 5; i++)
            Console.WriteLine("Next Value is " + ob.GetNext());

        Console.WriteLine("\nStarting at 100");
        ob.SetStart(100);
        for (int i = 0; i < 5; i++)
            Console.WriteLine("Next Value is " + ob.GetNext());
    }
}
*/